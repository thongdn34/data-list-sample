import styled from 'styled-components';

export const StickyFilter = styled.div`
  position: sticky;
  top: 20px;
`
export const ResetButton = styled.div`
  display: flex;
  align-items: center;
  cursor: pointer;
`